import React from 'react'
import Search from '../shared/Search'

const Items = (props) => {
    console.log(props)
    return (
        <div> Items Page
            <Search />
        </div>
    )
}

export default Items